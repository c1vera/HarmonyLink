import styled from "styled-components";

interface ButtonProps {

}

const ButtonEach = styled.button `
  
`

const Button: React.FC<ButtonProps> = (props) => {
  return (
    <ButtonEach></ButtonEach>
  )
}

export default Button;
